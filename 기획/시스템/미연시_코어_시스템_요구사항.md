# 미연시 코어 시스템 요구사항

## 1. 목적

이 문서는 현재 스토리 기획을 실제 게임 시스템으로 구현하기 위해 필요한 코어 로직을 정리한다.

현재 StoryFlow는 Scene / Shot / Branch / Transition 기반의 스토리 실행 구조를 제공한다. 미연시 게임을 만들기 위해서는 그 위에 날짜, 이벤트 선택, 캐릭터 진행도, 플래그, 루프, 엔딩 기록 같은 게임 상태 관리 계층이 필요하다.

## 2. 현재 StoryFlow 기준

`StoryFlow`의 현재 기능 요약:

- `StorySceneAsset` 하나가 하나의 Scene 흐름을 가짐
- Scene 안에는 Shot / Branch / Transition 그래프가 있음
- Shot은 실제 진행 단계
- Branch는 여러 출력 중 다음 경로 선택
- Transition은 다음 Scene으로 이동
- 저장/복원 최소 단위는 `FStoryFlowRef = SceneID + ShotID`

현재 구조는 장면 실행에는 적합하지만, 미연시의 장기 상태를 직접 표현하기에는 부족하다.

따라서 추가 코어 시스템은 StoryFlow를 대체하지 않고, StoryFlow가 참조할 수 있는 `스토리 상태 레이어`로 설계하는 것이 좋다.

## 3. 게임 구조 요약

현재 스토리 구조:

```text
첫 회차 D-32 ~ D-26: 기존 히로인 관계 기준점
D-25: 은하연 전학 / 루프 시작점
D-23: 담임의 축제 이틀 일정 공식 공지 / 2학년 5반의 반 단위 참여 확정
D-25 ~ D-19: 하연 적응 / 관계 변화 시작
D-18 ~ D-12: 2학년 5반 반행사 `쉬어가 카페` 확정 / 축제 준비 진입 / 관계 확장
D-11 ~ D-5: 관계 균열 심화 / 사고 단서 구체화
D-4 ~ D-2: 기억 회복 심화 / 관계 정리 클라이맥스 / 하연 고백 예고
D-1 목요일: 축제 1일차 / 부스 정상 운영 / 하연과 `쉬어가 카페` 운영으로 관계 강화
D-Day 금요일: 축제 2일차 / 부스 축소 운영 후 히로인 선택 / 사고 재현 / 엔딩 분기
D+3 월요일: 방학식 예정 / 실패 루프에서는 도달하지 못하는 다음 월요일 / 진엔딩에서는 일요일 기상 후 참석
```
표기 규칙:

- `D-Day`는 내부 날짜 상태, 조건 평가, 엔딩 분기 기준으로만 사용한다.
- 게임 내 대사, 칠판, 일정표, UI에는 `D-Day`를 직접 노출하지 않는다.
- 게임 내 표기는 `축제 1일차/첫날/목요일`, `축제 2일차/마지막 날/금요일`, `폐장 후`를 사용한다.

루프 이후에는 D-32 ~ D-26 구간을 반복하지 않고 D-25 월요일에서 시작한다. D+3 월요일 방학식은 일정상 존재하지만, 실패 루프에서는 D-Day 이후 그 날짜에 도달하지 못한다. 진엔딩에서는 D-Day 금요일 밤 이후 토요일을 날짜 표기로 보여주지 않고 일요일 오후 병실로 점프하며, 재윤이 깨어난 뒤 D+3 월요일 방학식에 겨우 참석한다.

진엔딩 조건은 세 축이다.

1. 하연과의 상호 마음 확인
2. 기존 관계 정리
3. 사고 단서 확보

하연과 서로의 마음을 확인하지 못한 상태로 D-Day 금요일 밤에 도달하면, 재윤은 하연이 있던 사고 기억을 온전히 복구하지 못한다. 이 경우 폐장 후 학교 뒤편 야외 소무대에 혼자 선 사고로 재구성되고, D+3 월요일 방학식에 도달하지 못한 채 D-25 월요일로 루프한다.

## 4. 필수 런타임 상태

### 4.1 날짜 / 회차 상태

필요 상태:

| 상태 | 예시 | 설명 |
| --- | --- | --- |
| 현재 날짜 | `D-25`, `D-14`, `D-1`, `D-Day`, `D+3` | 이벤트 개방과 루프 시작점, 축제 1일차/2일차, 방학식 예정일 표시에 사용 |
| 현재 주차 | `Week1`, `Week2`, `Week3`, `FinalWeek` | 이벤트 단계 관리용 |
| 첫 회차 여부 | `bIsFirstRun` | 첫 회차 전용 1주 프롤로그 표시 여부 |
| 루프 횟수 | `LoopCount` | 반복 연출, 히든 엔딩 조건에 사용 |
| D-Day 진입 여부 | `bReachedDDay` | 엔딩 분기 트리거 |
| 방학식 예정 인지 여부 | `bKnowsVacationCeremony` | D+3 월요일 방학식 안내와 미루 멘토링 종료 불안에 사용 |

요구 기능:

- 날짜 진행
- 특정 날짜 범위 이벤트 개방
- 첫 회차와 루프 이후 시작점 분리
- D-1 도달 시 축제 1일차 부스 운영 이벤트와 D-Day 진입 전 최종 정산 처리
- D-Day 도달 시 축제 2일차 선택과 엔딩 조건 평가
- D+3 월요일 방학식은 예정일로 관리하되, 실패 루프에서는 실제 일반 진행 날짜로 진입하지 않음

### 4.2 캐릭터 진행도

필요 캐릭터:

- 은하연
- 한소하
- 백서린
- 나미루

각 캐릭터별 상태:

| 상태 | 설명 |
| --- | --- |
| 관계도 / 호감도 | 하연과의 상호 마음 확인과 일반 히로인 이벤트 진행에 사용 |
| 이벤트 단계 | Week별 이벤트 진행도 |
| 클라이맥스 해결 여부 | 진엔딩 조건 B에 사용 |
| 관계 균열 단계 | 미련, 회피, 의존, 기억 혼선 연출 강도에 사용 |
| 마지막 선택 날짜 | 병렬 이벤트 선택 균형 확인용 |

주의:

- 일반 히로인의 관계도는 연애 성공을 뜻하지 않는다.
- 일반 히로인 진행도는 `기존 관계 정리`와 `미련/회피/의존 해소`를 의미한다.

### 4.3 플래그 / 조건 상태

필요 플래그 예시:

| 플래그 | 설명 |
| --- | --- |
| `MetHayeon` | 은하연 전학 이벤트 완료 |
| `HayeonEvent_FirstTalk` | 하연 첫 대화 완료 |
| `HayeonEvent_TrustTalk` | 하연이 자기 속도와 마음을 말할 수 있을 만큼 신뢰가 쌓인 핵심 대화 완료 |
| `HayeonEvent_FestivalBoothDay1` | D-1 축제 1일차에 하연과 `쉬어가 카페` 부스 운영을 함께하며 관계 강화 완료 |
| `HayeonEvent_Premonition` | 하연이 직접 약속을 잡지 않고, 재윤이 하연의 조용한 동선과 속도를 이해함 |
| `HayeonMutualHeartConfirmed` | 하연과 재윤이 서로의 마음을 확인할 조건 충족 |
| `HayeonMissingFromAccidentMemory` | 하연 조건 부족으로 사고 기억에서 하연이 빠지고 혼자 소무대 사고로 재구성됨 |
| `SohaClimaxResolved` | 소하 클라이맥스 해결 |
| `SeorinClimaxResolved` | 서린 클라이맥스 해결 |
| `MiruClimaxResolved` | 미루 클라이맥스 해결 |
| `MiruMentoringEndingSoon` | 미루의 1학기 한정 멘토링 종료가 가까워졌음을 인지 |
| `VacationCeremonyAnnounced` | D+3 월요일 방학식 일정 공지 확인 |
| `SundayWakeUpAfterDDay` | 진엔딩에서 토요일 표기 없이 일요일 오후 병실에서 깨어남 |
| `AttendedVacationCeremony` | 진엔딩 이후 D+3 월요일 방학식 참석 |
| `DDayUnlockedTrueChoice` | 하연과의 상호 마음 확인, D-1 하연 부스 운영, D-2 말하지 못한 예감, 기존 관계 정리, 사고 단서 조건을 충족해 D-Day 진엔딩 선택지 개방 |

요구 기능:

- 플래그 설정 / 해제 / 조회
- Branch에서 플래그 기반 분기
- 여러 플래그 조합으로 선택지 개방

D-1 밤 정산과 D-Day 낮 보정의 구체 점수·우선순위 기준은 [D-1/D-Day 조건 수치화](../스토리/12_D-1_D-Day_조건_수치화.md)를 따른다. 해당 문서는 `HaYeonTrust`, `HaYeonPaceRead`, 기존 관계 `Resolved` 플래그, 사고 단서 수, `Avoidance`를 조합해 폐장 후 최종 분기를 평가하는 기준표다.

### 4.4 사고 단서 / 기억 조각

사고 단서는 진엔딩 조건에 직접 연결된다.

필요 단서:

| 단서 | 획득 경로 | 의미 |
| --- | --- | --- |
| 하연을 좋아했다는 사실 | 한소하 루트 | 감정 단서 |
| 소무대 임시 구조물 위험 | 백서린 루트 | 사고 환경 단서 |
| 출입금지 표지 / 고정끈 문제 | 나미루 루트 | 사고 원인 단서 |
| 고백 장소, 끊긴 문장, 혼자가 아니었던 사고 기억 | 하연 루트 | D-Day 기억 단서 |

요구 기능:

- 단서 획득
- 단서 보유 여부 조회
- 단서 수량 / 특정 조합 검사
- 루프 이후 단서 유지 여부 결정

권장:

- 단서는 단순 불리언보다 `MemoryFragment` 형태로 관리한다.
- UI에서 확인 가능한 `기억 조각` 목록으로 확장 가능하게 한다.

### 4.5 엔딩 기록

필요 상태:

| 상태 | 설명 |
| --- | --- |
| 본 엔딩 목록 | 엔딩 수집용 |
| 새드엔딩별 관람 횟수 | 반복 새드엔딩 압축 / 특수 대사 조건 |
| 총 새드엔딩 횟수 | 기억 혼선 / 루프 피로 / 히든 엔딩 조건 |
| 진엔딩 완료 여부 | 클리어 후 콘텐츠 조건 |
| 히든 엔딩 완료 여부 | 복구 이벤트 개방 조건 |

요구 기능:

- 엔딩 기록 저장
- 같은 엔딩 반복 횟수 증가
- 반복 횟수에 따라 연출 분기
- 엔딩 수집 UI로 확장 가능하게 설계

## 5. 핵심 시스템 요구사항

### 5.1 날짜 기반 이벤트 허브

미연시의 기본 루프는 날짜별 허브다.

예시 선택지:

```text
[하연과 시간을 보낸다]
[소하와 등교한다]
[서린을 돕는다]
[미루의 멘토링을 봐준다]
[축제 준비를 조사한다]
```

필요 기능:

- 현재 날짜와 캐릭터 진행도에 따라 선택 가능한 이벤트 목록 생성
- 이미 완료한 이벤트 제외 또는 압축판 제공
- 특정 이벤트 우선순위 처리
- 선택 결과로 StoryFlow Scene 시작

StoryFlow 연결:

- 날짜 허브는 별도 Scene으로 구성 가능
- 각 선택지는 Transition으로 이벤트 Scene에 진입
- 이벤트 종료 후 다시 허브 Scene으로 복귀

### 5.2 조건 기반 선택지

D-Day와 주요 클라이맥스에서는 조건에 따라 선택지가 달라진다.

예:

조건 부족:

```text
[아무 일도 없었던 것으로 한다]
[선택하지 않는다]
[여기에 남는다]
```

조건 충족:

```text
[하연에게 돌아간다]
```

완전 조건 충족:

```text
[다친 기억까지 안고 하연에게 돌아간다]
```

진엔딩 복귀 처리:

- 금요일 밤 D-Day 선택 이후 토요일 날짜 허브는 생성하지 않는다.
- 바로 일요일 오후 병실 Scene으로 전환한다.
- 재윤은 큰 외상은 없지만 알 수 없는 이유로 깨어나지 못했던 상태로 처리한다.
- 일요일 병실 Scene 이후 D+3 월요일 방학식 에필로그 Scene을 개방한다.

필요 기능:

- 선택지별 표시 조건
- 선택지별 비활성 조건 또는 숨김 조건
- 선택 시 상태 변경
- 조건 미충족 시 대체 선택지 제공

StoryFlow 연결:

- BranchTemplate에서 상태를 조회해 출력 인덱스를 선택할 수 있어야 함
- 또는 선택지 UI가 상태 조건을 평가하고, 결과를 StoryFlow에 전달해야 함

### 5.3 병렬 진행도 관리

한 명씩 공략하는 구조가 아니므로 병렬 상태가 필요하다.

필요 기능:

- 캐릭터별 이벤트 단계 저장
- 선택하지 않은 캐릭터의 관계 균열 단계 증가
- 특정 주차 이후 클라이맥스 이벤트 개방
- 하연과의 상호 마음 확인, D-1 하연 부스 운영 이벤트와 여러 캐릭터 조건이 D-Day 진엔딩 조건에 합산

권장 모델:

```text
CharacterProgress
- CharacterID
- AffectionOrBond
- EventStage
- DistortionLevel
- bClimaxResolved
- LastEventDay
```

### 5.4 루프 처리

루프는 메인 반복 플레이를 강요하는 시스템이 아니라, 실패 후 재도전 장치다.

필요 기능:

- D-Day 실패 시 D-25로 복귀
- D+3 월요일 방학식은 실패 루프에서 도달하지 못하는 예정일로 유지
- 첫 회차 전용 프롤로그는 반복하지 않음
- 기억 조각 / 엔딩 기록 / 일부 플래그 유지
- 일반 관계도와 날짜는 루프 시작점에 맞게 재초기화

유지할 상태:

- 엔딩 기록
- 기억 조각
- 히든 엔딩 여부
- 기억 혼선 / 루프 피로
- 일부 메타 플래그

초기화할 상태:

- 현재 날짜
- 일반 이벤트 진행도
- 일시적 선택 플래그
- D-Day 진입 상태

### 5.5 이벤트 재방문 압축

루프 피로도를 줄이기 위해 필요하다.

필요 기능:

- 이벤트 최초 관람 여부 저장
- 재관람 시 압축 이벤트로 전환
- 플레이어가 스킵/요약을 선택 가능
- 단, 중요한 선택지는 다시 제공

예:

- 최초 관람: 전체 대화와 연출
- 2회차: 핵심 문장 + 선택지만 표시
- 기억 조각 보유 시: 바로 분기 선택지 제공

### 5.6 기억 혼선과 루프 피로

새드엔딩 반복과 히든 엔딩 조건에 사용한다.

필요 상태:

| 상태 | 설명 |
| --- | --- |
| `MemoryConfusion` | 기억 혼선과 루프 피로 정도 |
| `RepeatedSadEndingCount` | 같은 새드엔딩 반복 여부 |
| `IgnoredHayeonCount` | 하연 이벤트 무시 횟수 |

효과 예시:

- 날짜 표시 깨짐
- 배경음에 병원 기계음 증가
- 선택지 문구 변경
- 히든 세계 붕괴 엔딩 개방

### 5.7 히든 엔딩 복구 상태

히든 엔딩 후 `망가진 D-25` 복구 이벤트가 필요하다.

필요 기능:

- 히든 엔딩 후 루프 시작 Scene 변경
- 복구 조각 수집 상태 저장
- 하연 이름표 획득 후 정상 D-25 복구

복구 조각:

- 소하의 팔찌 또는 오래된 사진
- 서린의 점검표 조각
- 미루의 키링 또는 고정끈
- 하연의 이름표

## 6. 데이터 설계 후보

### 6.1 Story State

```text
StoryState
- CurrentDay
- CurrentWeek
- bIsFirstRun
- LoopCount
- MemoryConfusion
- CurrentPhase
- CharacterProgressList
- Flags
- MemoryFragments
- EndingRecords
```

### 6.2 Event Definition

```text
StoryEventDefinition
- EventID
- DisplayName
- CharacterID
- WeekRange
- RequiredFlags
- RequiredFragments
- RequiredProgress
- SetFlagsOnComplete
- AddFragmentsOnComplete
- NextSceneID
- bCanCompressOnReplay
```

### 6.3 Ending Definition

```text
EndingDefinition
- EndingID
- EndingType
- RequiredFlags
- RequiredFragments
- RequiredCorruption
- SceneID
- bCountsForCollection
```

## 7. 최소 구현 범위

먼저 필요한 최소 범위:

1. 날짜 상태
2. 캐릭터별 진행도
3. 플래그 저장/조회
4. 기억 조각 저장/조회
5. 조건 기반 선택지
6. D-1 축제 1일차 정산 및 D-Day 엔딩 조건 평가
7. 루프 시 D-25 복귀
8. 엔딩 기록

이 범위만 있어도 현재 스토리의 핵심 구조는 구현 가능하다.

## 8. 후순위 구현 범위

나중에 추가해도 되는 기능:

1. 이벤트 재방문 압축
2. 엔딩 수집 UI
3. 기억 혼선과 루프 피로에 따른 연출 변화
4. 히든 엔딩 복구 전용 UI
5. 기억 조각 도감
6. 자동 이벤트 추천 / 알림

## 9. StoryFlow 추가 기능 후보

현재 StoryFlow는 Scene/Shot/Branch 실행에 강점이 있으므로, 미연시 시스템은 별도 상태 계층으로 두는 것이 좋다.

추가 후보:

1. `StoryStateSubsystem`
   - 날짜, 플래그, 기억 조각, 캐릭터 진행도, 엔딩 기록 관리
2. `StoryEventRegistryAsset`
   - 날짜/조건별 이벤트 정의 관리
3. `StoryConditionEvaluator`
   - 조건 기반 선택지와 Branch 평가
4. `StoryLoopManager`
   - 루프 초기화, 유지/초기화할 상태 구분
5. `StoryEndingManager`
   - 엔딩 조건 평가와 엔딩 기록

## 10. 다음 설계 과제

1. 실제 코드에 이미 존재하는 저장/플래그/이벤트 관련 시스템 확인
2. `StoryState` 데이터 구조 확정
3. 이벤트 정의 에셋을 만들지, DataTable로 갈지 결정
4. 조건 기반 선택지를 StoryFlow Branch로 처리할지 UI 선택지 시스템에서 처리할지 결정
5. 최소 구현 범위 기준으로 개발 순서 작성
